export function likedList(el) {
    if(el.classList.contains('dnone')) {
        el.classList.remove('dnone');
    } else {
        el.classList.add('dnone');
    }
}
export function getLiked() {
    
}